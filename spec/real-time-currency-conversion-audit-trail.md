# Spec

<!-- Business layer + Known Constraints — carried through exactly as given, not written by you -->

**Title:** Real-Time Currency Conversion & Audit Trail

**Business Idea:** Treasury operations teams at our enterprise customers
currently convert currency using manual lookups against a third-party
portal — slow, and it leaves no record for compliance. We need instant,
self-serve conversion inside our own app, and because these are regulated
transactions, every conversion must be reconstructable for an auditor on
demand, not pieced together afterward from emails or spreadsheets.

**Persona:** Treasury operations analysts at enterprise customers, who
process multiple cross-border settlements a day and are personally
accountable if a conversion can't be justified during an audit.

**User Need:** See a trustworthy converted amount the moment I enter it
— and be able to pull up any past conversion, with its rate and timestamp,
the moment an auditor asks for it.

**Known Constraints:**
- Frontend must read `VITE_API_URL` dynamically at container runtime using
  a generic entrypoint script placeholder replacement; replace the placeholder token
  `__VITE_API_URL__` inside the built `index.html` (or equivalent) with the runtime
  `VITE_API_URL` value.
- Backend must read database configurations exclusively from runtime
  environment variables.
- Backend must query external currency conversion data exclusively using a base URL from an environment variable (`CURRENCY_API_BASE_URL`), defaulting to an unauthenticated open-source endpoint like Frankfurter (`https://frankfurter.dev`).
- The application must not hardcode third-party API response schemas; utilize flexible JSON property mapping or an abstraction layer to isolate internal compliance tracking from external JSON structure changes (such as `rates` vs `conversion_rates`).
- The audit trail record must capture the exact execution timestamp generated by your backend server alongside any date or sequence markers returned by the external provider, ensuring complete sub-second audit reconstruction even if the provider only exposes a calendar date.
- External HTTP calls must handle failures gracefully with fallback values or clear domain exceptions rather than bubbling up raw network or serialization errors.
- CI/CD pipeline must inject placeholders safely at deployment time without
  exposing sensitive data in container layers.
- Restricted to specific stack versions: React (Vite/Node 24.*) and C#
  (**backend must target `net10.0` for build/test; CI must use `dotnet-version: 10.0.x`**).
- Must adhere to a strict folder layout (`src/frontend` and `src/backend`).
- A .NET solution file must always be generated at `src/backend/OuterloopLabApi.sln` via `dotnet new sln` + `dotnet sln add`, with the backend project added to it, and CI/build/test commands must invoke this `.sln` rather than the `.csproj` directly.
- Deploy as a single Azure Container App using the sidecar pattern to host
  both frontend and backend.
- Authenticate to Cosmos DB exclusively using token-based authentication via the pre-assigned User-Assigned Managed Identity, instantiated using `DefaultAzureCredential` configured with the specific `ManagedIdentityClientId`. Absolutely zero connection strings, master keys, or account keys are permitted in code, configurations, or logs.
- Utilize `Microsoft.Azure.Cosmos` and `Azure.Identity` packages for
  secure, token-based database interactions, including data-plane CRUD operations like adding and deleting items.
- Include `Azure.ResourceManager` and `Azure.ResourceManager.CosmosDB` packages to programmatically provision the Cosmos DB database and container via the control plane inside the application startup lifecycle (`Program.cs`) before the web application runs, utilizing the same token-based Managed Identity credentials for authentication.
  - ARM provisioning is best-effort; Managed Identity RBAC for ARM may differ from data-plane RBAC.
  - CRUD (and data-plane creation if needed) must be done via the Cosmos SDK (data-plane) using token-based Managed Identity credentials; access keys must not be used.
  - After ARM attempt, startup must run token-authenticated data-plane `CreateDatabaseIfNotExistsAsync` and `CreateContainerIfNotExistsAsync` and only proceed if the database/container are available (either already exist or were created). If token-authenticated data-plane create-if-not-exists fails, startup must fail.
- Map and bind all configuration values, including `AZURE_MANAGED_IDENTITY_CLIENT_ID`, endpoint URLs, and resource names, directly from the environment variables **using the exact keys** defined in `docs\CONTAINER_ENVIRONMENT_VARIABLES.md` without using local settings configuration files as fallbacks.

<!-- Technical layer — this is what you add -->

**Current Context:** Assuming the standard file layout implied by the Known Constraints, the implementation should be centered in exact paths under the required roots: `src/backend/OuterloopLabApi.sln`, `src/backend/Program.cs`, `src/backend/OuterloopLabApi/OuterloopLabApi.csproj`, `src/backend/OuterloopLabApi/Controllers/CurrencyConversionsController.cs`, `src/backend/OuterloopLabApi/Models/CurrencyConversionRequest.cs`, `src/backend/OuterloopLabApi/Models/CurrencyConversionResult.cs`, `src/backend/OuterloopLabApi/Models/AuditTrailRecord.cs`, `src/backend/OuterloopLabApi/Services/CurrencyProviderClient.cs`, `src/backend/OuterloopLabApi/Services/CurrencyConversionService.cs`, `src/backend/OuterloopLabApi/Repositories/AuditTrailRepository.cs`, `src/frontend/index.html`, `src/frontend/src/main.tsx`, `src/frontend/src/App.tsx`, `src/frontend/src/api/currencyApi.ts`, and `src/frontend/src/components/AuditTrailSearch.tsx`. If equivalent files already exist, update those exact responsibilities rather than creating parallel implementations.

**In Scope:**
- Add a backend POST conversion endpoint that accepts source amount, source currency, and target currency; calls the configured external rate provider; computes the converted amount; and persists an audit record to Cosmos DB before returning the response.
- Add a backend audit lookup endpoint that returns a previously stored conversion record by server-generated conversion/audit identifier so an auditor can reconstruct the transaction on demand.
- Add backend validation for positive monetary amounts and three-letter currency codes, with invalid requests rejected before any external API or Cosmos call is attempted.
- Add an internal provider-mapping abstraction so the backend can normalize upstream rate/date payloads into the app's own compliance/audit model without coupling persisted records to a specific provider JSON shape.
- Add frontend UI in `src/frontend/src/` for entering a conversion request, submitting it to the API, displaying the converted amount immediately, and then retrieving a saved audit record by its identifier.
- Display the server-generated audit identifier, provider date marker, applied rate, and exact backend execution timestamp in the frontend after a successful conversion.

**Out of Scope:**
- Bulk conversion uploads, spreadsheet import/export, or batch settlement workflows.
- Multi-provider routing, provider failover between several upstream currency services, or provider-specific admin configuration UI.
- Historical rate backfill, scheduled refresh jobs, or offline caching of exchange rates.
- Authentication/authorization redesign for end users beyond whatever the existing app already does.
- Auditor-facing PDF generation, email delivery, or external reporting integrations.

**Feature Constraints:**
- Use `IHttpClientFactory` for outbound currency-provider requests so timeout/retry policy wiring stays centralized and testable.
- Normalize provider data into an internal DTO/model that contains at least `Rate`, `ProviderDateOrSequenceMarker`, and the currencies involved; the UI and Cosmos persistence layer must depend on that internal model rather than raw provider JSON.
- Generate the audit/conversion identifier on the backend as a new opaque ID (for example, a GUID) and return it to the frontend; the frontend must not invent or mutate audit IDs.
- Store monetary inputs, rate, and converted amount as `decimal` values, and store the backend execution timestamp as UTC `DateTimeOffset` so the exact sub-second server time can be round-tripped.
- Surface upstream/provider or persistence failures as explicit RFC 7807 `ProblemDetails` responses from the API, not raw exception text, with a stable error contract the frontend can render.
- Keep currency codes normalized to uppercase ISO-style 3-letter strings at validation, persistence, and response boundaries.

**Expected Outcome:** A user can submit a request such as `amount=100.00`, `from=USD`, `to=EUR` from the frontend and receive a successful API response that includes a backend-generated audit ID, the normalized applied rate, the converted amount, the provider's date/sequence marker, and the backend execution timestamp. For example, if the provider returns a rate of `0.92` with provider date `2026-08-01`, the response should contain values equivalent to `sourceAmount: 100.00`, `sourceCurrency: "USD"`, `targetCurrency: "EUR"`, `rate: 0.92`, `convertedAmount: 92.00`, `providerDateOrSequenceMarker: "2026-08-01"`, and `executedAtUtc: "2026-08-01T14:23:10.1234567Z"`, and that same record must be retrievable later by audit ID from Cosmos DB with matching persisted values. If the external provider call fails, the conversion endpoint must return a `ProblemDetails` response with HTTP 503 and a domain message indicating the conversion rate could not be obtained, rather than exposing a raw network or JSON parsing exception.

**Expected Agent Output:** OpenCode should produce implementation edits only under `src/backend` and `src/frontend` that add the conversion API, audit retrieval API, Cosmos-backed audit persistence, provider-response normalization, runtime-configured frontend API consumption, and the corresponding UI flow; it should also update or create any required tests/build wiring so backend build/test execution targets `src/backend/OuterloopLabApi.sln` and the sample success/error outcomes above are verifiable.

**Suggested Intent For OpenCode:** Implement this Spec in the existing app by updating only the required files under `src/backend` and `src/frontend`, and explicitly respect every Known Constraint exactly as written — not just the feature-specific ones — including `net10.0`, `src/backend/OuterloopLabApi.sln`, runtime environment-variable binding with the exact documented keys, runtime `__VITE_API_URL__` placeholder replacement, sidecar-compatible frontend/backend structure, Managed Identity token-authenticated Cosmos provisioning/access, and graceful external HTTP failure handling. Deliver a backend currency-conversion endpoint plus audit-record lookup endpoint, a normalized provider-mapping layer, Cosmos persistence of audit records with exact backend execution timestamps, frontend conversion/audit lookup UI, and tests or verification steps that prove the sample request/response and 503 `ProblemDetails` behavior.
